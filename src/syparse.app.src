{application, syparse,
    [{description, "LALR grammar based Solidity parser"},
        {vsn, "1.0.0"},
        {registered, []},
        {applications, [kernel, stdlib]},
        {mod, {syparse, []}},
        {env, []},
        {modules, [sylexer, syparse, syparse_fold, sytest]}]}.
